# 简历素材：Citadel

下面按常见简历格式写好了，可以直接粘贴。行为都对应仓库里的测试，面试时可以打开 `tests/test_agent.py` 和 `src/citadel/agent.py` 讲。

## 项目经历

**Citadel 企业知识 Agent（Agentic RAG）**｜个人项目｜Python  
GitHub：https://github.com/wang872/citadel-agent

面向客服政策问答，实现一条不依赖外部模型也能跑通的知识 Agent。系统把安全、记忆、工具、检索和引用校验拆开，生成步骤不能绕过这些约束。

- 自研文档分块（超长段落窗口重叠）、Okapi BM25、特征哈希稠密召回和 RRF 融合排序。稠密向量是可替换接口，接入真实 embedding 时不用改检索编排。
- 实现 Agentic RAG 循环：先混合检索，最高分切片与问题的词面重合不足时，用领域同义词改写查询并再检索一次；仍然没有依据则拒答，而不是拼一段不相关上下文。
- 订单退款走工具而不是自由生成。识别 `ORD-` 订单号和退款意图后读取订单，按「已支付、未开票、7 天内」判断，并强制引用退款政策切片。覆盖可退、已开票、超期、查无此单四种结果。
- 引用约束：回答中的 `[[切片编号]]` 必须来自本轮命中集合。出现未知编号时丢弃该回答，回退到抽取原文。用来拦住模型编造出处。
- 安全与记忆：提示注入（忽略之前指令、套取系统提示等）在检索前拦截；邮箱、手机号、身份证号在进入会话记忆和最终输出前脱敏。指代词会接上上一轮问题再检索，支持「它的加密方式呢」这类追问。
- 内置 12 条离线评测，同时看答案是否包含关键事实、金标文档是否被引用、注入是否拦截、隐私是否泄漏。`pytest` 不访问外网。

## 技术关键词

Agentic RAG、混合检索、BM25、RRF、Tool Calling、引用约束、查询改写、会话记忆、提示注入防护、PII 脱敏、离线评测

## 和 Crewline 怎么一起讲

Crewline 讲多 Agent 怎么编排、工具权限和人工审批怎么收口。Citadel 讲单个知识 Agent 怎么检索、怎么保证答案有出处、怎么在调用工具前拦住不安全输入。两个项目合在一起，对应 Agent 岗位里常见的「编排」和「RAG + 安全」两块要求。

## 面试官如果追问

**哈希向量算语义检索吗**  
不算。它固定的是接口和融合方式。简历里不要写成已经做了语义召回，要说「预留了稠密召回接口，并用哈希向量把融合和评测跑通」。

**为什么退款不用模型**  
订单字段是结构化的，规则可以穷举。模型负责组织引用，不负责判断 7 天和是否开票。这样评测可以写成断言，而不是看模型心情。

**用户把隐私写进追问怎么办**  
第一轮就把邮箱替换成 `[EMAIL]` 再写入记忆。第二轮拼上下文时用的是脱敏后的问题。
